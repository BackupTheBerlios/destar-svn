TODO for Asterisk 1.4:

*Change ${TIMESTAMP} variable to ${STRFTIME(${EPOCH},,%Y%mNaVH%M%S)}
*AgentCallBacklogin is depricated->  http://svn.digium.com/view/asterisk/tags/1.4.2/doc/queues-with-callback-members.txt?view=markup
*pbx_functions are now splited in several modules.
*n+101 return code is depricated.
*Use rfc2833compensate option in sip.conf to comunicate with 1.2 asterisk systems.
*Set CALLERID(ANI) as well as CALLERID(num).
*Check musicclass and  mohsuggest stuff.
